<div class='user-details'>
  <div class='user-image'><%= image_tag user.photo %> </div>
  <div class='user-post'>
    <% if user.present? %>
      <h2><%= link_to_unless_current user.name, user %></h2>
      <p>Number of posts: <%= user.posts.count %> </p>
    <% end %>
  </div>
</div>
